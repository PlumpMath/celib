CMAKE_MINIMUM_REQUIRED(VERSION 2.5)
PROJECT(celib)
ADD_LIBRARY(celib SHARED string.c)
INSTALL_FILES(/include/celib/ "\.h")
INSTALL_TARGETS(/lib RUNTIME_DIRECTORY /bin SharedLibraryTarget)
#INSTALL(TARGETS celib RUNTIME DESTINATION bin LIBRARY DESTINATION lib ARCHIVE DESTINATION libstatic)
